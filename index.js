/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Tessarak from './src/Tessarak';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Tessarak);
